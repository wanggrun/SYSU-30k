During training, checkpoints and logs are saved in this folder. If you want to save your weights somewhere else, please use symlink, for example:

```shell
ln -s /data1/wangguangrun/GraphReID_work_dirs  work_dirs
```
